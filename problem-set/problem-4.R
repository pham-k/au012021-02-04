# Given a vector a, find the index of largest element

find_max <- function(a) {
  index <- 1
  # your code here
  
  print(paste("Index of largest element is ", index))
}

a <- (runif(5))
a
find_max(a)